## Project Structure

- `backend/` – Contains the backend code (API, business logic, etc.)
- `frontend/` – Contains the frontend code (UI, client-side logic, etc.)

## Getting Started

1. Clone the repository:
   ```bash
   git clone <repo-url>
   ```

2. Follow the setup instructions in the `backend/` and `frontend/` directories to run the project locally.
